# /Economy

Standing knowledge base — macro data, sectors, and recurring economic themes (inflation, circular debt, exchange rate, IMF programs). One file per theme, e.g. `circular-debt.md`, `exchange-rate.md`.

Updated whenever an episode's research touches these themes, so figures and context compound across episodes instead of resetting each time.
